<section id="command.groups.image"><title>Image Commands</title>
	
	<para>Save bitmap and vector images of the current view. The GUI is not required in order to save images -- using these commands from the command-line, for example, works just as well (models still have a current view, even without the GUI, and can be rotated, translated etc. just as if they were in the GUI).</para>

	<section id="command.ref.savebitmap"><title>savebitmap</title>
		<indexterm><primary>commands</primary><secondary>savebitmap</secondary></indexterm>
		
		<para><emphasis>Syntax:</emphasis></para>
		<funcsynopsis>
			<funcprototype>
				<funcdef>void <function>savebitmap</function></funcdef>
				<paramdef>string <parameter>format</parameter></paramdef>
				<paramdef>string <parameter>filename</parameter></paramdef>
			</funcprototype>
			<funcprototype>
				<funcdef>void <function>savebitmap</function></funcdef>
				<paramdef>string <parameter>format</parameter></paramdef>
				<paramdef>string <parameter>filename</parameter></paramdef>
				<paramdef>int <parameter>width</parameter></paramdef>
				<paramdef>int <parameter>height</parameter></paramdef>
			</funcprototype>
			<funcprototype>
				<funcdef>void <function>savebitmap</function></funcdef>
				<paramdef>string <parameter>format</parameter></paramdef>
				<paramdef>string <parameter>filename</parameter></paramdef>
				<paramdef>int <parameter>width</parameter></paramdef>
				<paramdef>int <parameter>height</parameter></paramdef>
				<paramdef>int <parameter>quality</parameter></paramdef>
			</funcprototype>
		</funcsynopsis>
		
		<para>Saves the current view as a bitmap image. Allowable values for <emphasis>format</emphasis> are:</para>
		<table frame='all' pgwide='1' tocentry='0'><title>Bitmap image formats</title>
			<tgroup cols='2' align='left' colsep='1' rowsep='1'>
				<thead>
					<row>
						<entry>Shortname</entry>
						<entry>Format</entry>
					</row>
				</thead>
				<tbody>
					<row>
						<entry>bmp</entry>
						<entry>Windows Bitmap</entry>
					</row>
					<row>
						<entry>jpg</entry>
						<entry>Joint Photographic Experts Group</entry>
					</row>
					<row>
						<entry>png</entry>
						<entry>Portable Network Graphic</entry>
					</row>
					<row>
						<entry>ppm</entry>
						<entry>Portable Pixmap</entry>
					</row>
					<row>
						<entry>xbm</entry>
						<entry>X11 Bitmap</entry>
					</row>
					<row>
						<entry>xpm</entry>
						<entry>X11 Pixmap</entry>
					</row>
				</tbody>
			</tgroup>
		</table>
		
		<para>If <emphasis>width</emphasis> and <emphasis>height</emphasis> are not specified the current dimensions of the view are used (800x600 if no GUI is present). The <emphasis>quality</emphasis> option determines the compression used on saved images, affecting, for example, the quality and size of jpegs and pngs, and should be an integer between 1 and 100 (with 100 being the best compression).</para>
		
		<para>For example:</para>
		<synopsis>
savebitmap(&quot;bmp&quot;, &quot;test.bmp&quot;);
		</synopsis>
		<para>saves the current view to a file 'test.bmp'.</para>
		<synopsis>
savebitmap(&quot;png&quot;, &quot;big.png&quot;, 5000, 5000, 10);
		</synopsis>
		<para>saves an enormous, highly uncompressed png.</para>

	</section>

	<section id="command.ref.savemovie"><title>savemovie</title>
		<indexterm><primary>commands</primary><secondary>savemovie</secondary></indexterm>
		
		<para><emphasis>Syntax:</emphasis></para>
		<funcsynopsis>
			<funcprototype>
				<funcdef>void <function>savemovie</function></funcdef>
				<paramdef>string <parameter>filename</parameter></paramdef>
				<paramdef>string <parameter>format</parameter></paramdef>
				<paramdef>int <parameter>width</parameter> = -1</paramdef>
				<paramdef>int <parameter>height</parameter> = -1</paramdef>
				<paramdef>int <parameter>quality</parameter> = -1</paramdef>
				<paramdef>int <parameter>firstframe</parameter> = 1</paramdef>
				<paramdef>int <parameter>lastframe</parameter> = &lt;last&gt;</paramdef>
				<paramdef>int <parameter>interval</parameter> = 1</paramdef>
			</funcprototype>
		</funcsynopsis>
		
		<para>Saves a movie of the trajectory associated to the current model. Note that a valid movie encoder must be installed (such as <ulink url="http://www.mplayerhq.hu">mencoder</ulink>) and arguments must be set in the program preferences (see the <link linkend="command.types.preferences">Prefs</link> variable). Aten will first save a series of png images of the width, height and quality specified to the temporary directory (also specified in the preferences) before passing them all to the provided encoder command.  The encoder arguments should contain both the text 'FILES' and 'OUTPUT' - when it comes to running the commane, Aten will substitute 'FILES' for a wildcard list of image files, and 'OUTPUT' for the target movie filename.  Note that the format of the output movie is entirely guided by the options passed to the encoder command.</para>
		
		<para>As with the <link linkend="command.ref.savebitmap">savebitmap</link> command, if <emphasis>width</emphasis> and <emphasis>height</emphasis> are not specified the current dimensions of the view are used (800x600 if no GUI is present), and the <emphasis>quality</emphasis> option determines the compression used on saved images. The other arguments are self-explanatory - <emphasis>firstframe</emphasis> and <emphasis>lastframe</emphasis> give the range of trajectory frames which will be saved, while the <emphasis>interval</emphasis> value determines the 'stride' between frames (i.e. '1' for every frame, '2' for every other frame, '10' for every tenth frame etc.).</para>
		
		<para>For example:</para>
		<synopsis>
savemovie(&quot;traj.mpg&quot;, 1024, 1024, -1, 50, 1000, 50);
		</synopsis>
		<para>saves a movie called 'traj.mpg' with size 1024x1024, beginning at frame 50 and writing every 50th frame until frame 1000.</para>

	</section>
	
</section>
