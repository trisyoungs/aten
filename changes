Updated git ignore. Reworked changeversion script to use GIT - ATENVERSION is now the only define present in src/main/version.h, and is updated (along with other values) only on a bump request to changversion or commit.
