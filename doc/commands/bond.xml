<section id="commands.groups.bonding"><title>Bonding Commands</title>
	
	<para>Create bonds and perform automatic bonding operations.</para>

	<section id="command.ref.augment"><title>augment</title>
		<indexterm><primary>commands</primary><secondary>augment</secondary></indexterm>
		<para><emphasis>Syntax:</emphasis></para>
		<itemizedlist>
			<listitem>
				<para> augment ( )</para>
			</listitem>
		</itemizedlist>
		<para>Augments bonds in the current model, automatically determining multiple bonds based on the valency of atoms, and aromaticity based on double bonds in rings.</para>
		
		<para>For example:</para>
		<synopsis>
			augment();
		</synopsis>
		
		<note><title>See also:</title>
			<itemizedlist mark='opencircle'>
				<listitem>
					<para> ((Methods: Augment)) for a description of the rebonding algorithm implemented in Aten^</para>
				</listitem>
			</itemizedlist>
		</note>
	
	</section>
	
	<section id="command.ref.bondtolerance"><title>bondtolerance</title>
		<indexterm><primary>commands</primary><secondary>bondtolerance</secondary></indexterm>
		<para><emphasis>Syntax:</emphasis></para>
		<itemizedlist>
			<listitem>
				<para> double bondtolerance ( )</para>
			</listitem>
			<listitem>
				<para> double bondtolerance ( double tol )</para>
			</listitem>
		</itemizedlist>
		<para>Adjust the bond calculation tolerance. It may often be necessary to tweak the bond tolerance in order to get Aten to recognise ((Patterns)) within models correctly. The current or new bond tolerance is returned.</para>
		
		<para>For example:</para>
		<synopsis>
			bondtolerance(1.20);
		</synopsis>
		<para>sets the bonding tolerance to 1.2.</para>
		<synopsis>
			double tol = bondtolerance();
		</synopsis>
		<para>retrieve the current bond tolerance.</para>
		
		<note><title>See also:</title>
			<itemizedlist mark='opencircle'>
				<listitem>
					<para> ((Methods: Rebond)) for a description of the rebonding algorithm implemented in Aten^</para>
				</listitem>
			</itemizedlist>
		</note>
		
	</section>
	
	<section id="command.ref.clearbonds"><title>clearbonds</title>
		<indexterm><primary>commands</primary><secondary>clearbonds</secondary></indexterm>
		<para><emphasis>Syntax:</emphasis></para>
		<itemizedlist>
			<listitem>
				<para> clearbonds ( )</para>
			</listitem>
		</itemizedlist>
		<para>Delete all bonds in the current model.</para>
		
		<para>For example:</para>
		<synopsis>
			clearbonds();
		</synopsis>
	
	</section>
	
	<section id="command.ref.clearselectedbonds"><title>clearselectedbonds</title>
		<indexterm><primary>commands</primary><secondary>clearselectedbonds</secondary></indexterm>
		<para><emphasis>Syntax:</emphasis></para>
		<itemizedlist>
			<listitem>
				<para> clearselectedbonds ( )</para>
			</listitem>
		</itemizedlist>
		<para>Delete all bonds in the current atom selection.</para>
		
		<para>For example:</para>
		<synopsis>
			clearselectedbonds();
		</synopsis>
	
	</section>
	
	<section id="command.ref.newbond"><title>newbond</title>
		<indexterm><primary>commands</primary><secondary>newbond</secondary></indexterm>
		<para><emphasis>Syntax:</emphasis></para>
		<itemizedlist>
			<listitem>
				<para> newbond ( int|atom i, int|atom j )</para>
			</listitem>
			<listitem>
				<para> newbond ( int|atom i, int|atom j, int bondtype )</para>
			</listitem>
			<listitem>
				<para> newbond ( int|atom i, int|atom j, string bondtype )</para>
			</listitem>
		</itemizedlist>
		<para>Create a new bond in the model between the specified atoms. The optional ''bondtype'' argument specified the type of bond: e.g. single (default), double, or triple. Alternatively, an integer number representing the bond order may be given.</para>
		
		<para>For example:</para>
		<synopsis>
			newbond(4, 5, &quot;double&quot;);
		</synopsis>
		<para>creates a double bond between the fourth and fifth atoms in the model.</para>
		<synopsis>
			newbond(1, 2, 3);
		</synopsis>
		<para>creates a triple bond between the first and second atoms in the model.</para>
		<synopsis>
			atom i,j;
			i = newatom(&quot;C&quot;, 0, 0, 0);
			j = newatom(&quot;H&quot;, 0, 1.08, 0);
			newbond(i, j, &quot;single&quot;);
		</synopsis>
		<para>creates a new single bond between two atoms, supplied as references.</para>
	
	</section>
	
	<section id="command.ref.newbondid"><title>newbondid</title>
		<indexterm><primary>commands</primary><secondary>newbondid</secondary></indexterm>
		<para><emphasis>Syntax:</emphasis></para>
		<itemizedlist>
			<listitem>
				<para> newbondid ( int i, int j )</para>
			</listitem>
			<listitem>
				<para> newbondid ( int i, int j, int bondtype )</para>
			</listitem>
			<listitem>
				<para> newbondid ( int i, int j, string bondtype )</para>
			</listitem>
		</itemizedlist>
		<para>Create a new bond in the model between atoms referenced by their temporary ID, set manually with the ((Commands: Atoms|#setid|setid)) command. Otherwise identical to the ((Commands: Bonding|#newbond|newbond)) command.</para>
	
	</section>
	
	<section id="command.ref.rebond"><title>rebond</title>
		<indexterm><primary>commands</primary><secondary>rebond</secondary></indexterm>
		<para><emphasis>Syntax:</emphasis></para>
		<itemizedlist>
			<listitem>
				<para> rebond ( )</para>
			</listitem>
		</itemizedlist>
		<para>Calculate bonding in the current model.</para>
		
		<para>For example:</para>
		<synopsis>
		rebond();
		</synopsis>
	
	</section>
	
	<section id="command.ref.rebondpatterns"><title>rebondpatterns</title>
		<indexterm><primary>commands</primary><secondary>rebondpatterns</secondary></indexterm>
		<para><emphasis>Syntax:</emphasis></para>
		<itemizedlist>
			<listitem>
				<para> rebondpatterns ( )</para>
			</listitem>
		</itemizedlist>
		<para>Calculate bonding in the current model, but restrict bond creation to between atoms in individual molecules of defined patterns.</para>
		
		<para>For example:</para>
		<synopsis>
			rebondpatterns();
		</synopsis>
		
		<para>'rebondpatterns' can be useful when molecules in a system are too close together to have the correct bonding detected. In such a case, bonds and any old patterns in the model may be cleared, new patterns created by hand, and then 'rebondpatterns' used to calculate bonds only between the atoms of individual molecules in the defined patterns in order to recreate the original molecules.</para>
		
		<para>For example:</para>
		<synopsis>
			clearbonds();                    # Delete existing bonds in model
			clearpatterns();                 # Delete any existing patterns in the model
			newpattern(&quot;benzene&quot;, 100, 12);  # Add new pattern: 100 molecules of benzene (12 atoms), followed by...
			newpattern(&quot;ether&quot;, 50, 15);     # ...50 molecules of diethyl-ether (15 atoms)
			rebondpatterns();                # Calculate bonds within individual benzene and ether molecules
		</synopsis>
	
	</section>
	
	<section id="command.ref.rebondselection"><title>rebondselection</title>
		<indexterm><primary>commands</primary><secondary>rebondselection</secondary></indexterm>
		<para><emphasis>Syntax:</emphasis></para>
		<itemizedlist>
			<listitem>
				<para> rebondselection</para>
			</listitem>
		</itemizedlist>
		<para>Calculate bonding between atoms in the current atom selection.</para>
		
		<para>For example:</para>
		<synopsis>
			rebondselection();
		</synopsis>
	
	</section>

</section>
