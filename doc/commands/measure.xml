<section id="command.groups.measuring"><title>Measuring Commands</title>
	
	<para>Make measurements of distances, angles, and torsion angles (dihedrals) in models. Note that there are two distinct sets of commands - those which 'measure' and those which calculate 'geometry'. The former create visible measurements within the model (which can then be viewed in the GUI), while the latter simply determine and return geometric values. Both sets take a variable number of arguments which determine whether a distance, angle, or torsion is measured/determined.</para>
	
	<section id="command.ref.clearmeasurements"><title>clearmeasurements</title>
		<indexterm><primary>commands</primary><secondary>clearmeasurements</secondary></indexterm>
		
		<para><emphasis>Syntax:</emphasis></para>
		<funcsynopsis>
			<funcprototype>
				<funcdef>void <function>clearmeasurements</function></funcdef>
				<void/>
			</funcprototype>
		</funcsynopsis>
		<para>Clear all measurements in the current model.</para>
		
		<para>For example:</para>
		<synopsis>
clearmeasurements();
		</synopsis>
		
	</section>

	<section id="command.ref.geometry"><title>geometry</title>
		<indexterm><primary>commands</primary><secondary>geometry</secondary></indexterm>
		
		<para><emphasis>Syntax:</emphasis></para>
		<funcsynopsis>
			<funcprototype>
				<funcdef>double <function>geometry</function></funcdef>
				<paramdef>atom|int <parameter>i</parameter></paramdef>
				<paramdef>atom|int <parameter>j</parameter></paramdef>
				<paramdef>atom|int <parameter>k = 0</parameter></paramdef>
				<paramdef>atom|int <parameter>l = 0</parameter></paramdef>
			</funcprototype>
		</funcsynopsis>
		<para>This command is a general measuring tool, able to measure distances, angles, and torsions in the model, depending on how many arguments are supplied. Note that, unlike the <link linkend="command.ref.measure">measure</link> command, the resulting measurement is <emphasis>not</emphasis> added to the Model's internal list, and thus will not be displayed in the model.</para>
		
		<para>For example:</para>
		<synopsis>
double rij[50];
for (int i=1; i&lt;=50; ++i) rij[i] = geometry(1,i);
		</synopsis>
		<para>calculates the distances between the first 50 atoms in the model and the first, regardless of whether they are bound or not</para>
		
	</section>
	
	<section id="command.ref.listmeasurements"><title>listmeasurements</title>
		<indexterm><primary>commands</primary><secondary>listmeasurements</secondary></indexterm>
		
		<para><emphasis>Syntax:</emphasis></para>
		<funcsynopsis>
			<funcprototype>
				<funcdef>void <function>listmeasurements</function></funcdef>
				<void/>
			</funcprototype>
		</funcsynopsis>
		<para>List all measurements in the current model.</para>
		
		<para>For example:</para>
		<synopsis>
listmeasurements();
		</synopsis>
		<para>prints out a list of measurements made so far.</para>
		
	</section>
	
	<section id="command.ref.measure"><title>measure</title>
		<indexterm><primary>commands</primary><secondary>measure</secondary></indexterm>
		
		<para><emphasis>Syntax:</emphasis></para>
		<funcsynopsis>
			<funcprototype>
				<funcdef>double <function>measure</function></funcdef>
				<paramdef>atom|int <parameter>i</parameter></paramdef>
				<paramdef>atom|int <parameter>j</parameter></paramdef>
				<paramdef>atom|int <parameter>k = 0</parameter></paramdef>
				<paramdef>atom|int <parameter>l = 0</parameter></paramdef>
			</funcprototype>
		</funcsynopsis>
		<para>This command is a general measuring tool, able to measure distances, angles, and torsions in the model, depending on how many arguments are supplied. Note that the resulting measurement is added to the Model's internal list, and will be displayed in the model. Also, note that measuring the same thing between the same atoms twice will remove the measurement from the Model.</para>
		
		<para>For example:</para>
		<synopsis>
double rij = measure(1, 2);
		</synopsis>
		<para>returns the distance between atoms 1 and 2.</para>
		<synopsis>
double theta = measure(10, 20, 30);
		</synopsis>
		<para>returns the angle between atoms 10, 20, and 30.</para>
		<synopsis>
double phi = measure(9, 8, 7, 6);
measure(9,8,7,6);
		</synopsis>
		<para>returns the torsion angle made between atoms 9, 8, 7, and 6, and then instantly removes it from the model by measuring it again.</para>
		
	</section>
	
	<section id="command.ref.measureselected"><title>measureselected</title>
		<indexterm><primary>commands</primary><secondary>measureselected</secondary></indexterm>
		
		<para><emphasis>Syntax:</emphasis></para>
		<funcsynopsis>
			<funcprototype>
				<funcdef>void <function>measureselected</function></funcdef>
				<paramdef>int <parameter>natoms</parameter></paramdef>
			</funcprototype>
		</funcsynopsis>
		<para>This command is a general measuring tool to measure all of one particular type of interaction (i.e. bond distances, angles, or torsions) within the current atom selection. The single argument specifies the type of interaction to calculate by specifying thu number of atoms involved in the interaction - i.e. 2, 3, or 4 for bond distances, angles, and torsions respectively.</para>
		
		<para>For example:</para>
		<synopsis>
measureselected(3);
		</synopsis>
		<para>calculates and displays all bong angles in the current atom selection.</para>
		
	</section>

</section>
